# ===========================================================
# .env.example — Template de variables de entorno
# NUNCA commitear el .env real con credenciales reales
# ===========================================================

# ---- MySQL / Database ----
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=seismic_db
DB_USER=root
DB_PASSWORD=atlas2025

# ---- Pipeline ----
# Sobreescribe valores de config.yaml si se define aquí
# PIPELINE_ENV=development     # development | staging | production

# ---- Logging (opcional) ----
# LOG_LEVEL=DEBUG
